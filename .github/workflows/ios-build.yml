# This workflow is superseded by build.yml which handles both Android and iOS.
# Kept here to avoid breaking any existing references; it has no triggers.
on: workflow_dispatch
name: iOS (legacy – use build.yml instead)
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Please use the Build workflow (build.yml) instead."
